fn main() {
    // TODO: Implement asynchronous server without `futures-rs` (unless they remove the TLS) that can be used by all the tools (IDEs, compiler, repl).
    // Don't really want to use json either, but it's probably best choice right now. GraphQL seems nice, but there doesn't seem to be good support for it.
    // Also LS integration, but it doesn't seem to support everything I need.
}
